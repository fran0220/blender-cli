#!/usr/bin/env bash
set -euo pipefail

# No authentication, services, or dependency repair is needed after resume.
# Compiler selection is restored by the login-shell hook installed during setup.
echo '[resume] Blender build environment restored from the orb snapshot'
